{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "debugger",
            "type": "go",
            "request": "launch",
            "mode": "debug",
            "program": "${workspaceRoot}/src/main.go",
            "envFile": "${workspaceRoot}/.env"
        }
    ]
}